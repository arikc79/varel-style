# Django
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL Database (основна БД)
DB_NAME=varel_style
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_SSLMODE=prefer
DB_CONN_MAX_AGE=60

# Fallback на SQLite (тільки для швидкого тестування без PostgreSQL)
# USE_SQLITE=True

# Make.com Webhook
MAKE_WEBHOOK_URL=https://hook.eu1.make.com/your-webhook
